<div class="jumbotron bg-warning my-3">
<h1 class="display-2 text-center">Devour Burgers!</h1>
<p class="lead text-center">Add a burger to the menu, eat the burger, and remake the burger!</p>
<hr class="my-4">

<div class="card-deck">

<div class="card shadow bg-light mb-3 p-3">
<h2 class="card-title text-center">Menu!</h2>
<hr class="my-4">
<ul class="card-body">
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>
</div>

<div class="card shadow bg-light mb-3 p-3">
<h2 class="card-title text-center">Remake the Burger!</h2>
<hr class="my-4">
<ul class="card-body">
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>
</div>

</div>

<div class="text-center card shadow bg-light mb-3 p-3">
    <h2>Add a burger to the menu HERE!</h2>
    <form class="create-form card-body">
        <div class="form-group">
            <label for="ca">Burger Name:</label>
            <input type="text" id="ca" name="burger_name">
        </div>
        <button type="submit" class="btn btn-secondary">Submit</button>
    </form>
</div>
</div>